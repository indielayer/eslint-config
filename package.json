{
  "private": true,
  "homepage": "https://indielayer.com/",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "eslint . --config=packages/eslint-config/index.js",
    "release": "pnpm -r publish --dry-run"
  },
  "dependencies": {
    "eslint": "^8.6.0"
  }
}
